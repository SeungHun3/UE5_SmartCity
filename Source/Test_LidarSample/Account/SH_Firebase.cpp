// Fill out your copyright notice in the Description page of Project Settings.


#include "SH_Firebase.h"

SH_Firebase::SH_Firebase()
{
}

SH_Firebase::~SH_Firebase()
{
}

void SH_Firebase::Test_Init_Firebase()
{
	//firebase::App* app;
	//app = firebase::App::Create(firebase::AppOptions());

	//firebase::auth::Auth* auth = firebase::auth::Auth::GetAuth(app);

	//// 자격 증명
	//firebase::auth::Credential credential = firebase::auth::GoogleAuthProvider::GetCredential(nullptr, nullptr);

	//firebase::Future<firebase::auth::User*> result = auth->SignInWithCredential(credential);

	//firebase::Future<firebase::auth::User*> Lastresult = auth->SignInWithCredentialLastResult();
	//if (Lastresult.status() == firebase::kFutureStatusComplete)
	//{
	//	if (Lastresult.error() == firebase::auth::kAuthErrorNone)
	//	{
	//		firebase::auth::User* user = *result.result();

	//		// user->display_name().c_str();
	//		
	//	}
	//	else
	//	{
	//		// result.error_message();
	//	}
	//}
	
}